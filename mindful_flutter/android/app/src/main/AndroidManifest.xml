<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ============ ESSENTIAL PERMISSIONS ============ -->
    <!-- Calendar Access - for Google Calendar integration -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    
    <!-- Internet - for API calls and OAuth -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Notifications - for focus mode alerts -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ============ OPTIONAL PERMISSIONS (Advanced Features) ============ -->
    <!-- Usage Stats - for screen time tracking -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    
    <!-- Do Not Disturb - for focus mode -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    
    <!-- Biometric - for app lock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    
    <!-- ============ BACKGROUND SERVICE PERMISSIONS ============ -->
    <!-- Foreground Service - for calendar monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    
    <!-- Background work scheduling -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="false">
        
        <!-- Main Activity - Flutter Activity for Android embedding v2 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Specifies an Android intent filter so Flutter apps can handle intents from other apps -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep linking intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="neubofy.app" />
                <data android:scheme="http" android:host="neubofy.app" />
            </intent-filter>
        </activity>
        
        <!-- ============ BACKGROUND SERVICES ============ -->
        <!-- Foreground Service for calendar monitoring -->
        <service
            android:name=".services.CalendarMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:description="@string/calendar_monitoring_service_description" />
        
        <!-- WorkManager for periodic calendar sync -->
        <provider
            android:name="androidx.work.impl.WorkManagerInitializer"
            android:authorities="${applicationId}.workmanager-init"
            android:enabled="false"
            android:exported="false" />
    </application>
</manifest>

